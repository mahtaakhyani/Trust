- In essence, while ==initial individual differences== play a role, the paper highlights that ==direct experience== and the observed sequence of a human's trust-related behaviors during an interaction ==are more critical== for an agent to dynamically model and ==predict a human teammate's trust== behavior.

- Thus, the ==cumulative effect of interaction==, and the ==**observed specific sequence of behaviors**== (measurable actions) that arise from that experience, can be used to model and predict future trust.

- As the interaction between a human and an autonomous agent progresses, the impact of initial trust-related traits, which might be gathered from pre-surveys, diminishes significantly. Instead, the human's direct experiences with the agent become the dominant factor in influencing their trust behaviors. This means that real-time interactions and the agent's performance during the interaction are more critical than pre-existing dispositions towards trust

> It uses "what happened" (as the actions of the user) to update its model, then predict "what will happen" (as the actions) to evaluate and update the model accordingly.

what will the trust be? nope. how anxious the user will be? using their 1. behavior 2. filled out surveys 3. biosignals.
so the question will be: "how anxious this person became?" and predict "how anxious they will become?" to update the model.